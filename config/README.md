# Configuración de builds

`builds.json` contiene el número del puesto, nombre del rol de Discord, equipamiento, habilidades y ruta de la imagen.

La numeración es también la clave del signup. Un número puede tener un único usuario asignado al mismo tiempo. Para builds que necesitan dos jugadores, crea dos entradas numeradas independientes o dos roles independientes.
